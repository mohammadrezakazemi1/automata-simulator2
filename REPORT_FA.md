
# گزارش کار پروژه شبیه‌ساز زبان‌ها و ماشین‌ها

## 1. معرفی پروژه

این پروژه یک نرم‌افزار دسکتاپ آموزشی برای درس نظریه زبان‌ها و ماشین‌ها است که با Python و PySide6 ساخته شده است. هدف پروژه تبدیل مفاهیم نظری درس به اجزای قابل مشاهده و قابل آزمایش است.

دو بخش اصلی برنامه عبارت‌اند از:

- آزمایشگاه اتوماتا: طراحی و شبیه‌سازی DFA، NFA و ε-NFA و تبدیل NFA به DFA
- آزمایشگاه گرامر: ورود و تحلیل CFG، محاسبه FIRST و FOLLOW، تجزیه رشته، درخت تجزیه، مشتق چپ‌ترین، حذف بازگشت چپ، فاکتورگیری چپ و جدول LL(1)

---

## 2. گرامر چیست؟

گرامر صوری روشی برای تعریف ساختار رشته‌های یک زبان است. یک گرامر معمولاً با چهار جزء G = (V, Σ, P, S) تعریف می‌شود:

- V: مجموعه Nonterminalها
- Σ: مجموعه Terminalها
- P: مجموعه Productionها
- S: نماد شروع

یک نمونه:

~~~text
S -> a A
A -> b A | ε
~~~

در این مثال S و A غیرپایانه، a و b پایانه و S نماد شروع است. ε نیز رشته تهی است.

---

## 3. الگوی ورود گرامر در برنامه

برنامه برای ورود گرامر از یک الگوی ساده استفاده می‌کند:

~~~text
Nonterminal -> production1 | production2
~~~

برای مثال:

~~~text
E -> E + T | T
T -> T * F | F
F -> ( E ) | id
~~~

علامت | چند Production جایگزین را نشان می‌دهد. برنامه این متن را به Productionهای مستقل تبدیل می‌کند تا الگوریتم‌ها بتوانند روی ساختار داده‌ای استاندارد کار کنند.

---

## 4. مدل داخلی Production

هر Production به صورت یک شیء با دو بخش نگهداری می‌شود:

~~~text
left = A
right = (b, A)
~~~

بنابراین گرامر از یک متن خام به یک مدل قابل پردازش تبدیل می‌شود.

سمت چپ Productionها برای تشخیص Nonterminalها استفاده می‌شود و نمادهایی که در سمت راست قرار دارند ولی Nonterminal نیستند، Terminal در نظر گرفته می‌شوند.

---

## 5. اعتبارسنجی گرامر

قبل از اجرای الگوریتم‌ها، برنامه گرامر را بررسی می‌کند. موارد مهم شامل:

1. خالی نبودن گرامر
2. معتبر بودن نام Nonterminalها
3. وجود نماد شروع
4. تشخیص Nonterminalهای غیرقابل دسترس
5. بررسی صحیح ε

این مرحله از اجرای الگوریتم روی داده نامعتبر جلوگیری می‌کند.

---

## 6. تحلیل رشته و Parse Tree

کاربر می‌تواند یک رشته ورودی مانند abb وارد کند. برنامه بررسی می‌کند آیا رشته توسط گرامر تولید می‌شود یا خیر.

در صورت پذیرش، یک Parse Tree ساخته می‌شود. درخت تجزیه ارتباط بین نماد شروع، Nonterminalها و Terminalهای رشته را نمایش می‌دهد.

برای نمونه گرامر:

~~~text
S -> a A
A -> b A | ε
~~~

و رشته:

~~~text
abb
~~~

ساختار مفهومی درخت:

~~~text
S
├── a
└── A
    ├── b
    └── A
        ├── b
        └── ε
~~~

---

## 7. الگوریتم تجزیه رشته

هسته گرامر از یک ساختار جدولی شبیه Earley Chart استفاده می‌کند.

هر وضعیت شامل این اطلاعات است:

- سمت چپ Production
- سمت راست Production
- موقعیت پردازش در سمت راست
- محل شروع Production

سه مرحله اصلی وجود دارد:

### Prediction

اگر نماد بعدی Nonterminal باشد، Productionهای مربوط به آن وارد Chart می‌شوند.

### Scanning

اگر نماد بعدی Terminal باشد، با نماد فعلی ورودی مقایسه می‌شود و در صورت تطابق، نقطه پردازش جلو می‌رود.

### Completion

وقتی یک Nonterminal کامل شد، به Production قبلی که منتظر آن بوده متصل می‌شود.

اگر در انتهای ورودی یک وضعیت کامل مربوط به نماد شروع وجود داشته باشد، رشته پذیرفته می‌شود.

---

## 8. Leftmost Derivation

برنامه پس از ساخت درخت می‌تواند مراحل مشتق چپ‌ترین را نمایش دهد.

در هر مرحله اولین Nonterminal از سمت چپ انتخاب و با Production مناسب جایگزین می‌شود.

نمونه مفهومی:

~~~text
S
a A
a b A
a b b A
a b b
~~~

این بخش ارتباط مستقیم میان الگوریتم برنامه و مفهوم Derivation در درس دارد.

---

## 9. FIRST

FIRST مشخص می‌کند چه Terminalهایی می‌توانند در ابتدای رشته تولیدشده از یک نماد قرار بگیرند.

مثلاً:

~~~text
S -> a A
~~~

پس:

~~~text
FIRST(S) = {a}
~~~

در برنامه FIRST با روش Fixed-Point Iteration محاسبه می‌شود:

1. برای Nonterminalها مجموعه خالی ساخته می‌شود.
2. Productionها بررسی می‌شوند.
3. Terminal مناسب وارد FIRST می‌شود.
4. اگر Nonterminal اول بتواند ε تولید کند، نماد بعدی نیز بررسی می‌شود.
5. این فرآیند تا زمانی ادامه پیدا می‌کند که هیچ مجموعه‌ای تغییر نکند.

---

## 10. FOLLOW

FOLLOW مشخص می‌کند چه Terminalهایی می‌توانند بعد از یک Nonterminal ظاهر شوند.

علامت پایان ورودی $ در FOLLOW نماد شروع قرار می‌گیرد.

مثلاً:

~~~text
S -> A b
~~~

باعث قرار گرفتن b در FOLLOW(A) می‌شود.

FOLLOW نیز با یک فرآیند تکراری محاسبه می‌شود، زیرا اطلاعات FOLLOW یک Nonterminal ممکن است به Nonterminal دیگری منتقل شود.

---

## 11. حذف Left Recursion

بازگشت چپ برای بعضی روش‌های Predictive Parsing مشکل ایجاد می‌کند.

نمونه:

~~~text
E -> E + T | T
~~~

الگوی کلی:

~~~text
A -> A α | β
~~~

به شکل زیر تبدیل می‌شود:

~~~text
A  -> β A'
A' -> α A' | ε
~~~

برنامه این تبدیل را برای بازگشت چپ مستقیم انجام می‌دهد.

---

## 12. Left Factoring

اگر چند Production با Prefix مشترک شروع شوند، برنامه می‌تواند Prefix را جدا کند.

مثلاً:

~~~text
S -> a A | a B
~~~

به ساختاری مشابه زیر تبدیل می‌شود:

~~~text
S  -> a SF
SF -> A | B
~~~

هدف این تبدیل کاهش ابهام هنگام انتخاب Production است.

---

## 13. جدول LL(1)

در LL(1) انتخاب Production بر اساس Nonterminal فعلی و یک Terminal از ورودی انجام می‌شود.

برای ساخت جدول، برنامه از FIRST و FOLLOW استفاده می‌کند.

برای Production:

~~~text
A -> α
~~~

ابتدا FIRST(α) محاسبه می‌شود. Production برای Terminalهای موجود در FIRST در جدول قرار می‌گیرد. اگر ε در FIRST وجود داشته باشد، از FOLLOW(A) برای تعیین خانه‌های جدول استفاده می‌شود.

اگر یک خانه جدول بیش از یک Production داشته باشد، برنامه آن را به عنوان Conflict گزارش می‌کند.

---

# بخش اتوماتا

## 14. مدل ماشین متناهی

ماشین متناهی در برنامه با اجزای زیر مدل می‌شود:

- States
- Alphabet
- Transitions
- Start State
- Final States

این اطلاعات در کلاس FiniteAutomaton نگهداری می‌شوند.

---

## 15. تشخیص DFA و NFA

برنامه برای بررسی Deterministic بودن ماشین دو شرط را بررسی می‌کند:

1. نباید ε-transition وجود داشته باشد.
2. برای هر زوج State و Symbol فقط یک مقصد وجود داشته باشد.

اگر این شروط برقرار باشند، ماشین DFA است؛ در غیر این صورت NFA یا ε-NFA است.

---

## 16. شبیه‌سازی DFA

الگوریتم DFA ساده است:

1. از Start State شروع می‌شود.
2. یک Symbol از ورودی خوانده می‌شود.
3. Transition مربوط پیدا می‌شود.
4. State جدید به State فعلی تبدیل می‌شود.
5. مراحل تا پایان ورودی ادامه پیدا می‌کنند.
6. اگر State نهایی باشد، رشته پذیرفته می‌شود.

برنامه مسیر کامل Stateها را نیز ذخیره می‌کند تا روی گراف نمایش داده شود.

---

## 17. شبیه‌سازی NFA

در NFA ممکن است یک Symbol چند مقصد داشته باشد. بنابراین برنامه به جای یک State، یک مجموعه از Stateهای ممکن را نگه می‌دارد.

ساختار کلی:

~~~text
Current States
      ↓
Move(symbol)
      ↓
New States
~~~

اگر ε-transition وجود داشته باشد، بعد از Move عملیات ε-Closure نیز انجام می‌شود.

---

## 18. ε-Closure

ε-Closure مجموعه تمام Stateهایی است که بدون مصرف Symbol ورودی از Stateهای فعلی قابل دسترسی هستند.

الگوریتم:

1. Stateهای اولیه وارد Closure می‌شوند.
2. در یک Stack قرار می‌گیرند.
3. هر State بررسی می‌شود.
4. مقصدهای ε آن پیدا می‌شوند.
5. Stateهای جدید به Closure و Stack اضافه می‌شوند.
6. عملیات تا زمانی ادامه پیدا می‌کند که State جدیدی باقی نماند.

این قسمت در برنامه با پیمایش گراف پیاده‌سازی شده است.

---

## 19. تبدیل NFA به DFA

مهم‌ترین الگوریتم بخش اتوماتا، Subset Construction است.

ایده اصلی:

هر State در DFA نماینده یک مجموعه از Stateهای NFA است.

مثلاً:

~~~text
{q0, q1, q3}
~~~

یعنی State متناظر در DFA نماینده حالتی است که NFA می‌تواند در یکی از q0، q1 یا q3 باشد.

---

## 20. مراحل Subset Construction

### مرحله اول: State شروع

ابتدا ε-Closure مربوط به Start State محاسبه می‌شود.

### مرحله دوم: ایجاد State

این مجموعه به عنوان اولین State DFA ثبت می‌شود.

### مرحله سوم: بررسی Alphabet

برای هر Symbol:

1. Move روی مجموعه انجام می‌شود.
2. ε-Closure مجموعه مقصد محاسبه می‌شود.
3. مجموعه جدید به عنوان State DFA در نظر گرفته می‌شود.

### مرحله چهارم: جلوگیری از تکرار

اگر مجموعه قبلاً ساخته نشده باشد، به Queue اضافه می‌شود.

### مرحله پنجم: تعیین Final State

اگر یک Subset حداقل یک Final State از NFA داشته باشد، State متناظر در DFA نیز Final خواهد بود.

این فرآیند تا خالی شدن Queue ادامه پیدا می‌کند.

---

## 21. نمایش گرافیکی

هدف پروژه فقط اجرای الگوریتم نیست؛ نتیجه نیز باید قابل مشاهده باشد.

در Designer:

- ایجاد State
- جابه‌جایی State
- انتخاب Start State
- انتخاب Final State
- رسم Transition

در Simulator:

- اجرای کامل رشته
- اجرای مرحله‌ای
- نمایش State فعلی
- نمایش مسیر اجرا
- نمایش Accepted یا Rejected

در NFA → DFA:

- نمایش Stateهای DFA
- نمایش Subsetهایی مانند {q0,q1}
- نمایش Transitionهای ماشین جدید

---

# معماری و کیفیت کد

## 22. جداسازی UI از الگوریتم

کد پروژه به گونه‌ای سازمان‌دهی شده که منطق الگوریتمی تا حد ممکن از رابط گرافیکی جدا باشد.

### main.py

مسئول رابط کاربری، Navigation، گراف، ورودی کاربر و اتصال دکمه‌ها به الگوریتم‌ها است.

### automata.py

مسئول مدل ماشین متناهی و الگوریتم‌های:

- DFA Simulation
- NFA Simulation
- ε-Closure
- Move
- تشخیص DFA/NFA
- Subset Construction

### grammar.py

مسئول:

- Parsing گرامر
- Validation
- FIRST
- FOLLOW
- Parsing رشته
- Parse Tree
- Leftmost Derivation
- Left Recursion
- Left Factoring
- LL(1)

### tests

تست‌های خودکار برای الگوریتم‌های اصلی پروژه در این پوشه قرار دارند.

---

## 23. تست‌های خودکار

موارد مهم تست‌شده:

- پذیرش رشته توسط DFA
- تبدیل NFA به DFA
- ε-NFA
- FIRST و FOLLOW
- پذیرش و رد رشته توسط CFG
- حذف Left Recursion
- ساخت جدول LL(1)
- تشخیص LL(1) Conflict
- Left Factoring

اجرای تست‌ها:

~~~powershell
python -m unittest discover -s tests -v
~~~

---

## 24. جمع‌بندی الگوریتم‌های پروژه

| مفهوم | الگوریتم یا روش |
|---|---|
| DFA | حرکت قطعی State به State |
| NFA | نگهداری مجموعه Stateهای ممکن |
| ε-NFA | ε-Closure + Move |
| NFA → DFA | Subset Construction |
| Grammar Parsing | تبدیل متن به Production |
| Validation | بررسی ساختار و Reachability |
| String Parsing | Earley-style Chart |
| Parse Tree | بازسازی درخت از Productionها |
| Leftmost Derivation | گسترش اولین Nonterminal |
| FIRST | Fixed-Point Iteration |
| FOLLOW | Fixed-Point Propagation |
| Left Recursion | تبدیل A → Aα | β |
| Left Factoring | استخراج Prefix مشترک |
| LL(1) | ساخت جدول با FIRST و FOLLOW |

---

## 25. نتیجه‌گیری

ساختار کلی پروژه را می‌توان به شکل زیر خلاصه کرد:

~~~text
مفهوم نظری
    ↓
مدل داده
    ↓
الگوریتم
    ↓
اجرای الگوریتم
    ↓
نمایش نتیجه
~~~

برای مثال، در NFA → DFA کاربر فقط یک عملیات تبدیل انجام نمی‌دهد؛ الگوریتم Subset Construction واقعاً اجرا می‌شود و Stateهای مجموعه‌ای و Transitionهای جدید ساخته می‌شوند.

در بخش گرامر نیز متن خام Productionها ابتدا به یک مدل ساختاری تبدیل می‌شود و سپس الگوریتم‌هایی مانند FIRST، FOLLOW، Parsing، حذف Left Recursion، Left Factoring و LL(1) روی همان مدل اجرا می‌شوند.

بنابراین پروژه ارتباط بین مفاهیم تئوری درس و پیاده‌سازی الگوریتمی آن‌ها را به شکل عملی نشان می‌دهد.


## وزن‌دهی به انتقال‌ها

برای توسعه قابلیت مدل‌سازی گراف، هر Transition علاوه بر مبدأ، مقصد و Symbol دارای Weight عددی غیرمنفی است. وزن در هسته ماشین ذخیره می‌شود و در رابط گرافیکی هنگام ایجاد یال از کاربر دریافت و روی یال نمایش داده می‌شود. وزن در منطق پذیرش DFA/NFA دخالت داده نمی‌شود و به‌عنوان ویژگی مستقل یال نگهداری می‌شود.
